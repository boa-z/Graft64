# Box64 watch

Box64 is intentionally not a runtime backend in G0-G3. Revisit only after upstream publishes a supported ARM64EC module and Wine's existing external emulator interface can load it without a project-owned ABI.
