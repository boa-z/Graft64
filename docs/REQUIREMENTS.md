# Requirements

The authoritative requirements are in [`graft64_codex_requirements.md`](../graft64_codex_requirements.md). This checkout implements the GRAFT-0001 subset (G0 host probe) and keeps G1+ runtime work out of scope until device feasibility is evidenced.
