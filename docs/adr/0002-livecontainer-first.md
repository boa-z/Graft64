# ADR 0002: LiveContainer first

LiveContainer is the first host adapter, but no path or undocumented magic value is hard-coded into Wine/FEX. Host detection is evidence-based and isolated to the probe layer.
